class UsersController < ApplicationController
  
  def new
    @new_user = User.new
  end
  
  def index
    @users = User.join("join (select users_id, title from positions) p on p.users_id=id").select("users.*, p.title as title")
  end
end